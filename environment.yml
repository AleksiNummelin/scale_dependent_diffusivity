name: scale_dependent_diffusivity
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (dont remove)
  - pytest
  # Examples (remove and add as needed)
  - jupyter
  - jupyterlab
  - xarray
  - xesmf
  - numpy
  - scipy
  - dask
  - netcdf4
  - matplotlib
  - cartopy
